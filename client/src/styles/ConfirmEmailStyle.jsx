import {styled} from '@mui/system'

const H1El = styled('h1')({
    color: 'white',
    textAlign: 'center'
});

const PEl = styled('p')({
    color: 'white',
    textAlign: 'center'
});



export {H1El, PEl}
